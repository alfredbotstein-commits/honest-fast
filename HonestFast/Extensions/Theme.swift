import SwiftUI

enum Theme {
    // Backgrounds
    static let background = Color(hex: "0D0D0F")
    static let surface = Color(hex: "1A1A1E")
    
    // Accent
    static let accent = Color(hex: "F5A623")
    static let accentDeep = Color(hex: "E8640F")
    
    // Status
    static let success = Color(hex: "7CB66A")
    static let teal = Color(hex: "5AC8C8")
    
    // Text
    static let textPrimary = Color(hex: "F2F0ED")
    static let textSecondary = Color(hex: "8A8A8E")
    
    // Timer
    static let timerTrack = Color(hex: "2A2A2E")
    
    // Light mode
    static let lightBackground = Color(hex: "FAFAF8")
    static let lightSurface = Color(hex: "F0EFEC")
    static let lightAccent = Color(hex: "E8960F")
    static let lightTextPrimary = Color(hex: "1A1A1E")
}
